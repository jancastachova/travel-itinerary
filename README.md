This is web application built with [Next.js](https://nextjs.org). This application lets user enter information in questionnaire page, which result in showing them the ideal destination. -> in later versions the whole itinerary.

## Live demo link

[https://travel-itinerary-beta-one.vercel.app/]

## Screenshots

-landing page - basic, simple style.
![alt text](./screenshots/first.png)
- Look at one questionnaire point
![alt text](./screenshots/second.png)
- Result page (The final recommendation generated by the scoring algorithm.)
![alt text](./screenshots/third.png)


## Tech stack
- Next.js
 - full-stack framework handling both frontend and backend
- TypeScript 
- Tailwind CSS
- PostGreSQL
 - via Prisma/Supabase

 ## Architecture
The core of this app is built on a custom scoring algorithm that matches user preferences to destinations stored in the database.
In MPV version, it simply loads database and compares the relative object properties with user preferences taken from the questionnaire. The function scoreDestination() will simply return the destination with the highest score.

After user finishes the questionnaire, API route is called that directs to the scoring algorithm, which gives the result and displays it to final result page. 

## How to run locally

```bash
# clone repo
# install npm
npm install
# set .env (copy database_url)
# Run database migrations(creates tables based on Prisma schema)
npx prisma migrate dev
# connect seed
npx prisma db seed
# run the application
npm run dev
```

Open [http://localhost:3000] with your browser to see the result
